name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Test (${{ matrix.platform }})
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest, macos-latest, ubuntu-latest]

    env:
      CPM_SOURCE_CACHE: ${{ github.workspace }}/.cpm-cache

    steps:
    # fetch-depth 0 is required: this job builds the extension, and
    # setuptools_scm derives the version from the tags.
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - uses: astral-sh/setup-uv@v7
      with:
        python-version: "3.12"
        enable-cache: true

    - name: Cache CPM packages
      uses: actions/cache@v6
      with:
        path: ${{ env.CPM_SOURCE_CACHE }}
        key: cpm-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'cmake/CPM.cmake') }}
        restore-keys: |
          cpm-${{ runner.os }}-

    - name: Install
      run: uv sync --extra test --extra arrow --verbose

    - name: Test
      run: uv run pytest
